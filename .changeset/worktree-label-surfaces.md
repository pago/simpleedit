---
"simpleedit": patch
---

Fix worktree labels in the workspace header button and session list to show `dir (branch)` when the directory name differs from the checked-out branch, matching the worktree popover list.

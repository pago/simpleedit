---
"simpleedit": patch
---

Worktrees panel now surfaces the on-disk directory name when it differs from the checked-out branch (e.g. a directory `improvements/` holding branch `main` shows as `improvements (main)`), so the path is no longer hidden. When the directory and branch match, only the single name is shown.
